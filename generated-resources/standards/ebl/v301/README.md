This is a partial re-import of the standard created as a side effect of building the new Java information model for the standard and exporting its Data Overview in the new format.

The ebl-end files are manually created as empty files to provide a correct baseline for 302 where END gets introduced.
